namespace Banco
{
     class Admin //:usuario
    {
        void listarContas()
        {
            /*
               var streamReader = new StreamReader("Clientes.json");

                var listarTodosClientes = streamReader.ReadToEnd();

                streamReader.Close();
            */ 
        }
        void excluirConta()
        {
            // manipular json
            // procurar como excluir algo expecifico do Json
            
        }
        void acessarConta()
        {
            // manipular json
            // procurar algo expecifico e printar json
        }
    }
}